package com.estadia.encuesta.Model;

import android.content.Context;
import android.net.ConnectivityManager;
import android.net.NetworkInfo;

public class Network {
    //private ConnectivityManager connectivityManager = (ConnectivityManager)getSystemService(Context.CONNECTIVITY_SERVICE);
    //private NetworkInfo networkInfo = connectivityManager.getActiveNetworkInfo();

    public boolean estadoConexion(){

        boolean mensaje=true;

        return mensaje;
    }
}
